#!/usr/bin/env ruby

require "bundler/setup"
require "cookie_cutter"

template_dir = ENV['CC_TEMPLATE_DIR']
if template_dir.nil?
  template_dir = '/Volumes/z/Dropbox/!Templates'
end

puts template_dir

o = CookieCutter::Oven.new 'test', 'blah', template_dir
puts o

puts 'c is for cookie'
